# Where the proxy forwards to. See docs/adr/0007 and next.config.js.
#
# Deliberately WITHOUT the NEXT_PUBLIC_ prefix: anything carrying it is inlined
# into the bundle the browser downloads. The rewrite runs on Next's server, so
# this value must stay server-side.
API_URL="http://localhost:3001"
